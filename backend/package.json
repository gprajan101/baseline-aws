{
    "name": "@baseline-aws/backend",
    "version": "0.1.0",
    "private": true,
    "description": "Lambda functions and backend logic for baseline-aws",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "test": "jest",
        "clean": "rm -rf dist",
        "lint": "eslint . --ext .ts"
    },
    "devDependencies": {
        "typescript": "~5.9.0",
        "@types/node": "^22.0.0",
        "@types/aws-lambda": "^8.10.0",
        "jest": "^29.0.0",
        "ts-jest": "^29.0.0",
        "@types/jest": "^29.0.0"
    }
}